"""
CLI Scripts

Entry points for running the system components:
    - generate_metadata.py  — Create .torrent.json from a file
    - run_tracker.py        — Start the tracker server
    - run_seeder.py         — Start a seeder peer
    - run_peer.py           — Start a leecher peer

These scripts are thin wrappers around the module code.
"""
